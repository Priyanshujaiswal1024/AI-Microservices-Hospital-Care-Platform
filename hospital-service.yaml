apiVersion: v1
kind: Service

metadata:
  name: hospital-service
  namespace: hospital
spec:
  selector:
    app: hospital-deployment

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

  type: NodePort